/**
 * Semantic version of the @diaspor/sdk package.
 *
 * Kept in sync with `package.json` by hand at release time; the build pipeline
 * verifies they match before publishing.
 */
export const VERSION = "0.1.0-alpha.1";
