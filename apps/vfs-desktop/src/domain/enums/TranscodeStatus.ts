/**
 * Transcode Status Enum
 *
 * Status values for transcoding operations
 */
export enum TranscodeStatus {
  Pending = 'pending',
  InProgress = 'in_progress',
  Completed = 'completed',
  Error = 'error',
}
