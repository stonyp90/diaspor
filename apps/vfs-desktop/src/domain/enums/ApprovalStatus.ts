/**
 * Approval Status Enum
 *
 * Status values for asset approval workflow
 */
export enum ApprovalStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
  Review = 'review',
}
