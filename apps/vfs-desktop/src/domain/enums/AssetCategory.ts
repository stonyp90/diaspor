/**
 * Asset Category Enum
 *
 * Categories for media assets in DAM/MAM workflows
 */
export enum AssetCategory {
  Raw = 'raw',
  Edit = 'edit',
  Final = 'final',
  Archive = 'archive',
  Proxy = 'proxy',
  Other = 'other',
}
