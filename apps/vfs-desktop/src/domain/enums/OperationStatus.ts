/**
 * Operation Status Enum
 *
 * Status values for tracked operations
 */
export enum OperationStatus {
  Pending = 'pending',
  InProgress = 'in_progress',
  Completed = 'completed',
  Failed = 'failed',
  Cancelled = 'cancelled',
}
