apiVersion: v1
kind: Service
metadata:
  name: {{ include "diaspor-api-server.fullname" . }}
  labels:
    {{- include "diaspor-api-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diaspor-api-server.selectorLabels" . | nindent 4 }}
